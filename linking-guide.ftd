-- ft.page-with-toc: `ftd` linking guide

-- ft.markdown:

We put all links without slash in front and slash in end.

Example:

In case of `[fpm build](/build/)` or `[fpm build](/build)` use like `[fpm build](build)`
-- ft.page: About `download-base-url`

`download-base-url` will optional in the command line.

if `download-base-url` is provided in the CLI we have to download all the files
from URL and then serve.

Make sure to download FPM.ftd file if `download-base-url` is provided, before
actual serving starts.

Need to replace `base` with `download-base-url` in package.


Note: This file is dummy file, I have to move it different places.
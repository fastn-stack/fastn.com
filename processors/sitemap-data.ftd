-- ft.page-with-toc: `sitemap-data` `$processor$`
toc: $config.sitemap.toc

`sitemap-data` processor should be used by document theme to get data to show to
end users. There is another processor,
[`get-sitemap-info`](processors/get-sitemap-info/), which shows sitemap data in
a different format, meant for site admins to understand the structure of their
site.

-- ft.code: Sitemap in `FPM.ftd`
lang: ftd

\-- fpm.sitemap:

# Section: section/url/

# If Section: Has A Colon In The Name
url: sectionURL

## Sub Section: sub/url/

## If Sub Section: Has A Colon In The Name
url: whatever

- ToC Item: toc/

-- ft.markdown:

The sitemap get parse into a record `sitemap-data`. This record is present in `fpm`
The record looks like this

-- ft.code: record `sitemap-data` (fpm.ftd)
lang: ftd

\-- record sitemap-data:
toc-item list sections:
toc-item list subsections:
toc-item list toc:


\-- record toc-item:
optional string title:
optional string url:
optional string font-icon:
optional string img-src:
boolean is-heading:
boolean is-disabled:
boolean is-active: false
toc-item list children:


-- ft.markdown:

The record `sitemap-data` contains sections, subsections and toc. For different document
these values would be different. We can get this values by calling the `get-sitemap`
processor which returns the value of `sitemap-data` type.

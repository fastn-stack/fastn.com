-- ft.page: `user-group-by-id` processor

`user-group-by-id` processor is used to find [groups](/dev/user-group/) by
group `id`. Each group have unique `id`.


-- ft.code:
lang: ftd

\-- fpm.user-group-compat group:
$processor$: user-group-by-id


This will initialize the `group` variable with group which are present in the
current FPM package by it's `id`.